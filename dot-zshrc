# Created by Zap installer
[ -f "${XDG_DATA_HOME:-$HOME/.local/share}/zap/zap.zsh" ] && source "${XDG_DATA_HOME:-$HOME/.local/share}/zap/zap.zsh"
plug "zsh-users/zsh-autosuggestions"
plug "zsh-users/zsh-syntax-highlighting"

alias ls="ls -G"

eval "$(/Users/sammcclenaghan/.local/bin/mise activate zsh)"
eval "$(starship init zsh)"
export BROWSER_PATH=/Applications/Helium.app/Contents/MacOS/Helium

# bun completions
[ -s "/Users/sammcclenaghan/.bun/_bun" ] && source "/Users/sammcclenaghan/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

eval "$(zoxide init zsh)"

# Pi
export PATH="/Users/sammcclenaghan/.local/share/mise/installs/node/25.9.0/bin:$PATH"

# nub
export PATH="$HOME/.nub/bin:$PATH"

# pnpm
export PNPM_HOME="/Users/sammcclenaghan/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end
